version: '3.8'

services:
  app:
    build: .
    container_name: nextjs_app
    restart: unless-stopped
    environment:
      - NODE_ENV=production
      - NEXT_PUBLIC_API_URL=https://digital-twin.page/api
    ports:
      - '3000'
